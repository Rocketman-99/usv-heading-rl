# 작업 일지 — David

**작성자: 작업자(David) 본인.** 이 파일에는 AI 가 쓴 내용을 넣지 않는다.

날짜와 한두 줄이면 충분하다. **막힌 것, 실패한 시도, 찜찜한 판단일수록 반드시 남긴다.**
잘 쓰려고 다듬지 말 것. 나중에 쓸모 있는 건 정리된 문장이 아니라 그때의 날것이다.

HANDOFF.md 11장의 두 질문에 대한 답이 여기서 나온다.

- **"가장 고생한 부분은 무엇인가?"** → 이 파일
- **"AI 를 어디까지 사용했는가?"** → 이 파일과 `worklog.md` 의 차이, 그리고 `decisions.md` 의 `AI 활용` 항목

> AI 에 위임한 작업의 기록은 `worklog.md` 에 따로 있다.
> 나눠 둔 이유는 `decisions.md` D-009 참조.

형식:
```
YYYY-MM-DD  한두 줄
```

무엇을 써도 된다. 참고용 예시:

```
2026-08-11  타각 18도 나오는 걸 보고 계단 10도가 큰가 싶었는데,
            실제로 배를 10도 돌리는 게 과한 조작인지 감이 안 온다.
2026-08-11  Kp 를 키우면 빨라지는 건 알겠는데 어디서 멈춰야 할지 기준이 없다.
            드론 때도 딱 이 지점에서 헤맸다.
2026-08-11  Python 으로 가기로 한 거, 사실 좀 아까웠다. 라이선스가 아니라
            조선 쪽 지원할 때 Simulink 안 쓴 게 걸린다.
```

써도 되는 것 — 기술적 막힘, 판단이 안 서는 지점, 시간을 날린 일,
예상과 달랐던 결과, 이 프로젝트를 왜 하는지에 대한 생각, 그날의 컨디션까지.

---

