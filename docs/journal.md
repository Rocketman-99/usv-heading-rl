# 작업 일지

날짜와 한두 줄이면 충분하다. **실패한 시도일수록 반드시 남긴다.**
면접 질문 "가장 고생한 부분은 무엇인가?" 의 답이 여기서 나온다.

형식:
```
YYYY-MM-DD  무엇을 했고 무엇이 안 됐는지 한두 줄
```

---

2026-08-07  프로젝트 착수. 저장소 구조, Nomoto 1차 모델, PID, 지표, 플롯 도구 작성.
            MATLAB/Octave 가 없는 환경이라 알고리즘을 Python 으로 그대로 옮겨 선검증.
            C1~C6 전부 통과 (C1 상대오차 4.9e-14, C3 T63 = 107.30 s = T).
            → 단, MATLAB 코드 자체는 아직 한 줄도 실행되지 않았다.
            로컬에서 `run_step_response_check.m` 을 먼저 돌려볼 것.

2026-08-07  C2(정상상태 r = K·δ)가 처음에 상대오차 2.48e-3 으로 실패했다.
            구현 버그를 의심했는데, 값이 정확히 e^(-6) 이었다.
            시뮬레이션 길이를 6T 로 잡아 지수항이 아직 안 죽은 것뿐이었다.
            → tEnd 를 10T 로 늘리니 4.5e-5 로 통과. 판정 기준 쪽이 틀렸던 사례.

2026-08-07  Nomoto K, T 문헌값 확보 과정에서 막힘. 논문 PDF 접근이 막힌 환경이라
            검색 스니펫만으로는 출처를 특정할 수 없었다. HANDOFF 9장(출처 없는 계수 금지)
            때문에 값을 추정해 쓰는 선택지는 버림.
            → GitHub 접근은 열려 있어서 Fossen 의 MSS 저장소를 직접 받아
            `mssExamples/exNomoto.m` 에서 K = 0.185, T1/T2/T3 = 118/7.8/18.5 확보.

2026-08-07  위 값을 그대로 믿지 않고 비선형 Mariner 모델을 적분해 대조했더니
            5 deg 타각에서 정상상태 선수각속도가 선형값의 **42%** 밖에 안 나왔다.
            처음엔 값이 틀렸거나 무차원 값인 줄 알았다.
            → `mariner.m` 의 선형 유체력 미계수로 r(s)/δ(s) 를 직접 유도하니
            K = 0.18499, T = 107.21 s 로 문헌값과 정확히 일치.
            값은 맞고, **비선형 감쇠 때문에 실선 운항 영역에서 선형 근사가 깨지는 것**이었다.

2026-08-07  비선형 모델의 δ → 0 극한 이득이 0.185 가 아니라 0.113 (비 0.61) 로 나와서
            또 막힘. 원인은 Chislett & Strøm-Tejsen 모델에 들어 있는 비대칭 항
            (Y0 = −4e-5, N0 = 3e-5) 이었다. 타각 0 에서도 r ≈ 0.17 deg/s 의 편향이 남는다.
            → 중앙차분 `(r(+δ) − r(−δ)) / 2δ` 로 편향을 제거하니 δ = 0.1 deg 에서 비 = 1.00.
            선형화가 정확함을 확인. 이 편향 항은 2단계 3자유도 모델에서 다시 문제가 될 수 있다.

            결론: 1단계 결과는 |δ| ≲ 1~2 deg 영역에서만 유효하다.
            이 한계를 숨기지 않고 `02_plant_model.md` 에 표로 남겼다.

2026-08-07  MATLAB 환경 확인 (R2023b, win64). Simulink 는 있는데
            **Reinforcement Learning Toolbox 가 설치·라이선스 모두 없음.**
            Control/Deep Learning/Optimization/Statistics 는 라이선스는 있고 설치만 안 된 상태.
            → 1단계 코드는 기본 MATLAB 기능만 쓰므로 플랜트 검증·베이스라인 튜닝은
            그대로 진행 가능. 막히는 건 학습 루프 하나뿐이다. D-002b 로 분리해 기록.
