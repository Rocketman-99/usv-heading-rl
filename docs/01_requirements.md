# 01. 요구사항 및 범위 정의

이 문서는 HANDOFF.md 1~4장을 기반으로 작성한 초안이다.
판단이 필요한 항목은 `[작업자 결정 필요]` 로 비워 두었다.

---

## 1. 목적과 포지션

**목적**: 취업 포트폴리오 (2026 하반기 공채, 방산·조선·자동차 설계/R&D 직무)

**대상**: 무인수상정(USV)의 침로 제어

**핵심 주장**: 사람이 시행착오로 맞추던 제어 게인을 정량적 절차로 최적화하고,
그 결과가 신뢰할 만한지를 검증한다.

**포지션**
최종 제어기는 검증 가능한 PID다. 강화학습은 제어기를 대체하지 않고
**게인을 찾는 최적화 도구**로만 쓴다.

이 포지션이 갖는 실무적 의미:

- 인증·심사 관점에서 최종 산출물이 4개 실수(K_p, K_i, K_d)이므로 검증 가능하다
- 학습이 실패해도 베이스라인 PID 라는 동작하는 제어기가 남는다
- "AI 가 조종한다"가 아니라 "AI 로 튜닝 공수를 줄인다"는 주장이므로 방어 가능하다

---

## 2. 해결하려는 문제

작업자는 드론, UAM(eVTOL), 수중로봇 세 프로젝트에서 제어 게인을 시행착오로 튜닝했다.
공통 한계는 두 가지다.

1. 조건이 바뀌면 처음부터 다시 맞춰야 한다
2. 왜 그 값이 좋은지 정량적으로 설명하기 어렵다

특히 UAM 사례에서는 1회 비행당 천이 시도가 3~5초에 불과해
파라미터와 결과의 상관을 판단할 시행 횟수 자체를 확보하지 못했다.

→ **시뮬레이션에서 시행 횟수를 확보하고, 좋은 게인의 기준을 수식으로 고정한다.**
   이 프로젝트가 하려는 일은 이것이다.

---

## 3. 기술 스택 및 환경

`src/checkEnv.m` 을 실행해 아래 표를 채운다.
**Claude Code 는 이 값을 추측해 채우지 않는다.**

| 항목 | 값 | 확인일 |
|---|---|---|
| MATLAB 버전 | `[작업자 결정 필요 — checkEnv 실행]` | |
| Simulink | `[사용 가능 여부]` | |
| Reinforcement Learning Toolbox | `[라이선스 확인]` | |
| Control System Toolbox | `[라이선스 확인]` | |
| Deep Learning Toolbox | `[라이선스 확인]` | |
| Optimization Toolbox | `[라이선스 확인]` | |

**툴박스를 쓸 수 없는 경우의 대안**

| 대안 | 장점 | 단점 |
|---|---|---|
| MATLAB + RL Toolbox | 조선·방산 업계 표준 환경. 포트폴리오 설득력 높음 | 라이선스 필요 |
| Python (Gymnasium + Stable-Baselines3) | 라이선스 무관, 알고리즘 선택지 넓음 | 업계 환경과 어긋남 |
| MATLAB (툴박스 없이 자체 구현) | 라이선스 최소, 알고리즘을 직접 설명 가능 | 구현·디버깅 부담 |

HANDOFF 3장 기준: **가능하면 MATLAB 우선.**
현재 저장소의 1단계 코드는 **기본 MATLAB 기능만 사용**하며 Simulink·RL Toolbox 를
요구하지 않는다. 따라서 플랜트 검증과 베이스라인 PID 튜닝까지는
툴박스 확인 결과와 무관하게 진행할 수 있다. GNU Octave 에서도 동작한다.

`[작업자 결정 필요]` — 위 표를 채운 뒤 학습 환경을 확정하고 `decisions.md` D-002 에 기록.

---

## 4. 단계별 범위와 완료 조건

각 단계는 그 시점에서 멈춰도 완결된 결과물이 남도록 설계되어 있다.
단계를 건너뛰거나 병합하지 않는다.

### 1단계 — Nomoto 파이프라인 (2~3주)

| 항목 | 내용 |
|---|---|
| 플랜트 | 1차 Nomoto `T·ṙ + r = K·δ` |
| 파라미터 | 문헌 표준 선형 값 (출처 명시) — `02_plant_model.md` |
| 산출물 | 베이스라인 PID vs RL 최적화 게인 성능 비교표 |
| 완료 조건 | 비교표가 나오고, **학습에 쓰지 않은 초기조건에서도 재현**될 것 |

진행 상황:

- [x] 저장소 구조, `.gitignore`
- [x] Nomoto 모델 구현 (`src/plant/`, `src/sim/`)
- [x] 계단 응답 검증 스크립트 (`run_step_response_check.m`)
- [x] PID 제어기 + 지표 + 플롯 도구
- [x] 파라미터 출처 문서화 및 재유도 검증
- [ ] MATLAB 에서 검증 스크립트 실행
- [ ] **베이스라인 PID 수동 튜닝 — 작업자 직접** ← 현재 여기서 멈춤
- [ ] 보상 함수 설계 — 작업자 직접
- [ ] 학습 루프 구성
- [ ] 미학습 조건 재현 확인

### 2단계 — 3자유도 모델 교체 (4~6주)

플랜트만 교체하고 1단계 파이프라인을 재사용한다.

- `M·ν̇ + C(ν)ν + D(ν)ν = τ`, `ν = [u, v, r]`
- 유체력 미계수는 공개 문헌의 표준 선형 데이터 (출처 명시)
- **학습 전에** 선회권 시험·Z자 조종시험을 재현해 모델 타당성을 먼저 확인
- 완료 조건: 모델 타당성이 문헌 값과 정성적으로 일치 + 3자유도에서도 RL 게인이 베이스라인 초과

### 3단계 — 외란 강건성 비교

- 파랑·조류를 τ 에 외력/모멘트로 인가
- 외란 조건별 게인 성능 비교
- 완료 조건: 외란 세기별 성능 저하 곡선 + 학습/미학습 조건 성능 차이표

---

## 5. 평가 지표

| 지표 | 정의 | 구현 | 보상 가중치 |
|---|---|---|---|
| 침로 오차 | 목표 침로와의 정상상태 편차 (RMS) | `headingMetrics.rmsErrSS` | `[작업자 결정 필요]` |
| 정정 시간 | 목표 침로의 ±5% 이내 진입까지 소요 시간 | `headingMetrics.tEnter` | `[작업자 결정 필요]` |
| 오버슈트 | 최대 초과 각도 | `headingMetrics.overshoot` | `[작업자 결정 필요]` |
| 러더 사용량 | 타각 변화율의 적분 ∫\|δ̇\|dt | `headingMetrics.rudderTravel` | `[작업자 결정 필요]` |

**가중치는 작업자가 직접 결정한다** (HANDOFF 5장, 7장).
이 가중치를 정하는 것이 "무엇을 좋은 제어라고 정의하는가"에 대한 답이며,
이 프로젝트의 핵심 설계 결정이다. 결정 시 `decisions.md` 에 근거를 남긴다.

참고 — 침로 오차만 최소화하면 러더가 지속 진동하는 해가 나오기 쉽다.
구동기 부하 항을 반드시 포함할 것.

**구현 시 주의 (Claude Code 가 확인한 사항)**

- `tEnter`(±5% 최초 진입) 와 `tSettle`(이후 이탈 없이 유지) 을 둘 다 계산한다.
  HANDOFF 7장 정의는 `tEnter` 이지만, 진입 후 다시 이탈하는 진동 응답에서는
  이 값이 성능을 과대평가한다. 두 값의 차이가 곧 잔류 진동의 크기다.
- "정상상태 구간"의 정의(기본: 뒤쪽 30%)는 검증 조건이므로 작업자 결정 사항이다.

---

## 6. 검증 요구사항

"학습이 잘 됐다"만으로는 채택하지 않는다 (HANDOFF 8장).

1. **베이스라인 대비 비교** — 동일 조건에서 수동 튜닝 PID 와 비교. 비교표 필수
2. **미학습 조건 재현** — 학습에 쓰지 않은 초기 침로/목표값/외란 크기에서 성능 유지 확인
3. **구동기 거동 확인** — 지표가 좋아도 타각이 진동하면 채택하지 않는다. 시간 응답 그래프 필수
4. **재현성** — 랜덤 시드를 바꿔 반복. 시드 하나로 결론 내지 않는다

세부 조건(어떤 초기 침로, 몇 개 시드, 어떤 외란 크기)은 `[작업자 결정 필요]`.
확정 시 `05_validation.md` 에 기록.

---

## 7. 범위 밖

- 잠수정(UUV) 구현 — 문서상 확장 과제로만 언급
- 경로 추종(path following), 충돌 회피 — 침로 제어만 다룬다
- 실선/실기 시험 — 시뮬레이션만
- RL 이 직접 타각을 내는 end-to-end 제어기 — 포지션에 반한다
