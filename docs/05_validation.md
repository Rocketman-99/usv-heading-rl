# 05. 검증 조건과 결과

> **검증 조건 설계는 작업자(David)가 직접 담당한다** (HANDOFF.md 5장).
> "어떤 조건에서 무엇을 확인할지"가 곧 이 프로젝트의 신뢰도를 결정한다.
>
> HANDOFF.md 8장: "학습이 잘 됐다"는 것만으로는 결과를 채택하지 않는다.

---

## 검증 4항목 체크리스트

- [ ] 1. 베이스라인 대비 비교
- [ ] 2. 미학습 조건 재현
- [ ] 3. 구동기 거동 확인
- [ ] 4. 재현성 (복수 시드)

네 항목을 **모두** 통과해야 결과를 채택한다.

---

## 1. 베이스라인 대비 비교

동일 조건에서 수동 튜닝 PID(03장)와 RL 최적화 게인을 비교한다.

### 조건

`[작업자 결정 필요]`

| 항목 | 값 |
|---|---|
| 계단 크기 | |
| 초기 조건 | |
| 시뮬레이션 길이 | |
| 판정 대역 | ±__% |
| 정상상태 구간 정의 | 뒤쪽 __% |

### 결과

`metrics_table([m_baseline, m_rl], ['baseline PID', 'RL gains'])` 출력을 붙인다.

`[결과 대기]`

| 지표 | 단위 | baseline PID | RL gains |
|---|---|---|---|
| 침로 오차 RMS (정상상태) | deg | | |
| 대역 진입 시간 | s | | |
| 정착 시간 | s | | |
| 오버슈트 | deg | | |
| 러더 사용량 ∫\|δ̇\| | deg | | |
| 최대 타각 | deg | | |

판정: `[작업자 판단]`

---

## 2. 미학습 조건 재현

학습에 쓰지 않은 초기 침로 / 목표값 / 외란 크기에서 성능이 유지되는가.

### 조건 분리

`[작업자 결정 필요]` — **학습 시작 전에** 정해 둘 것. 사후에 정하면 검증이 아니다.

| | 학습 사용 | 검증 전용 |
|---|---|---|
| 초기 침로 ψ₀ | | |
| 목표 침로 | | |
| 외란 (3단계) | | |

### 결과

`[결과 대기]`

| 조건 | 학습 사용 여부 | 침로 오차 RMS | 정착 시간 | 러더 사용량 |
|---|---|---|---|---|
| | | | | |

성능 저하율:

판정: `[작업자 판단]`

---

## 3. 구동기 거동 확인

**지표가 좋아도 타각이 지속 진동하면 채택하지 않는다** (HANDOFF 8장 3항).

- [ ] 시간 응답 3단 플롯(침로 / 선수각속도 / 타각)을 항상 함께 확인했는가
- [ ] 타각에 지속 진동이 없는가
- [ ] 타각 포화 시간 비율(`sat_frac`)이 허용 범위인가
- [ ] 타각 변화율이 구동기 한계에 계속 붙어 있지 않은가

`[결과 대기]` — `figures/`

관찰:

---

## 4. 재현성

랜덤 시드를 바꿔 학습을 반복했을 때 결과가 유사한가.
**시드 하나의 결과만으로 결론 내지 않는다** (HANDOFF 8장 4항).

### 조건

`[작업자 결정 필요]`

- 시드 개수:
- 시드 값:

### 결과

`[결과 대기]`

| 시드 | Kp | Ki | Kd | 침로 오차 RMS | 정착 시간 | 러더 사용량 |
|---|---|---|---|---|---|---|
| | | | | | | |

게인 산포 (평균 ± 표준편차):
성능 산포 (평균 ± 표준편차):

판정: `[작업자 판단]`

> 게인 값 자체는 흩어져도 성능이 비슷할 수 있다 (해가 평평한 골짜기에 있는 경우).
> 그 경우 "게인이 재현되지 않는다"가 아니라 "성능이 게인에 둔감하다"가 결론이며,
> 이는 오히려 실무적으로 좋은 성질이다. 둘을 구분해서 서술할 것.

---

## 5. 1단계 모델 한계의 명시

`02_plant_model.md` 에서 확인된 사항:

- 1차 Nomoto 는 |δ| ≳ 5 deg 에서 실제 선회 능력을 2배 이상 과대평가한다
- 따라서 **1단계 결과는 작은 침로 변경 영역에서만 유효하다**

검증 결과를 서술할 때 이 한계를 함께 적는다. 숨기면 면접에서 무너진다.
2단계(3자유도 모델)가 필요한 이유가 바로 이것이다.

`[작업자 작성]` — 이번 검증에서 사용한 계단 크기가 요구한 최대 타각:
