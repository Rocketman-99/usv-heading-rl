# usv-heading-rl — 프로젝트 인수인계

이 문서는 Claude Code 세션 시작 시 가장 먼저 읽는 문서입니다.
작업 중 판단이 필요할 때 이 문서의 원칙을 기준으로 삼습니다.

> ⚠️ `[TODO]` 표시된 항목은 작업자(David)가 직접 채워야 합니다. Claude Code가 임의로 채우지 않습니다.

---

## 1. 프로젝트 정의

**목적**: 취업 포트폴리오 (2026 하반기 공채, 방산·조선·자동차 설계/R&D 직무)

**대상**: 무인수상정(USV)의 침로 제어

**핵심 주장**: 사람이 시행착오로 맞추던 제어 게인을 정량적 절차로 최적화하고, 그 결과가 신뢰할 만한지를 검증한다.

**포지션 (중요)**
이 프로젝트는 "AI로 제어기를 대체하는" 프로젝트가 아니다.
최종 제어기는 검증 가능한 PID이며, 강화학습은 **게인을 찾는 최적화 도구**로만 사용한다.
모든 설계 판단은 이 포지션을 유지하는 방향으로 내린다.

---

## 2. 배경 및 동기

작업자는 다음 프로젝트에서 제어 게인을 시행착오로 튜닝한 경험이 있다.

- **드론 제작**: LiDAR 값 튐으로 고도 진동 발생 → PX4 파라미터를 하나씩 조정하며 안정화
- **UAM (eVTOL)**: ArduPilot QuadPlane 천이 속도 파라미터를 매뉴얼 초기값에서 시행착오로 조정. 검증 환경 제약(1회 비행당 천이 시도 3~5초)으로 파라미터와 결과의 상관을 판단하지 못함
- **수중로봇**: 호버링 시 수류에 밀리는 문제를 제어 파라미터를 손으로 맞춰가며 대응

공통 한계는 두 가지다.
1. 조건이 바뀌면 처음부터 다시 맞춰야 한다
2. 왜 그 값이 좋은지 정량적으로 설명하기 어렵다

또한 위 프로젝트들은 모두 **기본 조종이 사람의 RC 조작**이었고, 자동제어는 고도 유지·호버링 같은 특정 미션 구간에만 개입했다. 이 프로젝트는 그 자동화 범위를 임무 전체로 넓히는 방향의 첫 단계에 해당한다.

---

## 3. 기술 스택 및 환경

- MATLAB `[TODO: 버전]`
- Simulink `[TODO: 사용 가능 여부]`
- Reinforcement Learning Toolbox `[TODO: 라이선스 확인]`
- Git / GitHub (public 저장소)

> 툴박스를 사용할 수 없는 경우 Python(Gymnasium + Stable-Baselines3) 대안을 검토한다. 단, MATLAB/Simulink가 조선·방산 업계 표준 환경이므로 가능하면 MATLAB을 우선한다.

---

## 4. 단계별 범위

각 단계는 **그 시점에서 멈춰도 완결된 결과물이 남도록** 설계되어 있다.
이 구조는 의도된 것이며, 임의로 단계를 건너뛰거나 병합하지 않는다.

### 1단계 — Nomoto 파이프라인 (2~3주)

전체 파이프라인을 가장 단순한 플랜트로 끝까지 한 번 완주한다.

- 플랜트: Nomoto 1차 모델 `T·ṙ + r = K·δ`
- 파라미터 K, T는 문헌의 표준 선형 값 사용 (출처 명시 필수)
- PID 베이스라인 튜닝 → 성능 기록
- 강화학습 게인 최적화 루프 구성
- 평가 지표 계산 및 시각화
- 학습 외 조건 재현 확인

**완료 조건**: 베이스라인 PID와 RL 최적화 게인의 성능 비교표가 나오고, 학습에 쓰지 않은 초기조건에서도 재현될 것

### 2단계 — 3자유도 모델 교체 (4~6주)

플랜트만 교체하고 1단계 파이프라인을 재사용한다.

- 플랜트: 수평면 3자유도 조종운동방정식 `M·ν̇ + C(ν)ν + D(ν)ν = τ`, `ν = [u, v, r]`
- 유체력 미계수는 공개 문헌의 표준 선형 데이터 사용 (출처 명시 필수)
- **학습 전에** 선회권 시험·Z자 조종시험을 시뮬레이션으로 재현해 모델 타당성을 먼저 확인
- 이후 1단계와 동일한 절차로 게인 최적화

**완료 조건**: 모델 타당성 검증 결과가 문헌 값과 정성적으로 일치하고, 3자유도에서도 RL 게인이 베이스라인을 넘을 것

### 3단계 — 외란 강건성 비교

- 파랑·조류를 `τ`에 외력/모멘트로 인가
- 외란 조건별로 게인 성능 비교
- 잔잔한 조건에서만 유효한 게인과 외란 하에서도 유지되는 게인을 구분해 제시

**완료 조건**: 외란 세기별 성능 저하 곡선과, 학습 조건 대비 미학습 조건에서의 성능 차이표

---

## 5. 역할 경계 ★ 가장 중요

이 프로젝트의 목적은 "동작하는 코드"가 아니라 **"작업자가 설명할 수 있는 결과물"**이다.
따라서 아래 경계를 반드시 지킨다.

### Claude Code가 담당

- Simulink 모델 및 MATLAB 스크립트 구현
- 강화학습 학습 루프, 환경 래퍼, 콜백 구성
- 데이터 처리, 로깅, 시각화, 결과 테이블 생성
- 디버깅, 리팩터링, 보일러플레이트
- 문헌 계수·수식의 구현 및 단위 확인
- 문서 초안 작성 (단, 판단이 들어가는 항목은 빈칸으로 남길 것)

### 작업자(David)가 직접 담당 — 대행 금지

- **베이스라인 PID 게인 튜닝** (수동으로 직접 수행)
- **보상 함수의 항 구성과 가중치 결정**
- **검증 조건 설계** (어떤 조건에서 무엇을 확인할지)
- **결과의 채택 여부 판단**
- `docs/decisions.md` 작성

### 처리 방식

위 항목에 해당하는 작업이 필요해지면 Claude Code는 **코드를 실행하지 말고 멈춘다.**
선택지와 각각의 trade-off를 제시하고 작업자의 결정을 기다린다.
작업자가 결정하면 그 값을 코드에 반영하고, 근거를 `decisions.md`에 남기도록 안내한다.

---

## 6. 저장소 구조 및 문서화 규칙

```
usv-heading-rl/
  README.md
  HANDOFF.md              (이 문서)
  pyproject.toml          패키지 정의, pytest 설정
  requirements.txt
  docs/
    01_requirements.md    요구사항 및 범위 정의
    02_plant_model.md     플랜트 모델과 파라미터 출처
    03_baseline_pid.md    베이스라인 튜닝 과정과 결과
    04_rl_design.md       보상 함수, 알고리즘, 하이퍼파라미터
    05_validation.md      검증 조건과 결과
    decisions.md          설계 결정 로그
    journal.md            작업 일지 — 작업자 본인 기록
    worklog.md            작업 로그 — AI에 위임한 작업 (D-009)
  src/usv_heading/        플랜트, 시뮬레이터, PID, 평가 지표
  scripts/                검증·튜닝 실행 스크립트
  tests/                  pytest. CI가 매 푸시마다 실행
  matlab/                 MATLAB 구현 (보류 — D-002b)
  .github/workflows/      CI
  results/                그래프, 요약 지표 (대용량 원본 제외)
  figures/
  .gitignore
```

1단계는 Python으로 진행한다 (D-002b). 2단계 환경은 1단계 완료 후 다시 정한다.

### decisions.md 형식

결정을 내린 **직후** 한 건씩 추가한다. 형식은 고정한다.

```
## D-003 플랜트 모델 수준
- 날짜: 2026-08-12
- 상황: 침로 제어 대상 모델을 정해야 함
- 선택지: Nomoto 1차 / 3자유도 조종운동모델
- 결정: Nomoto로 시작, 파이프라인 완성 후 3자유도로 교체
- 근거: 계수 확보 난이도와 학습 시간을 고려. 중간에 멈춰도 결과물이 남는 구조를 우선
- AI 활용: 구현은 위임, 모델 수준 판단은 직접
- 재검토 시점: 파이프라인 완성 후
```

`AI 활용` 항목을 반드시 포함한다. 어디까지 위임하고 어디부터 직접 판단했는지가 기록으로 남아야 한다.

### journal.md / worklog.md 형식

두 파일 모두 날짜와 한두 줄이면 충분하다. 실패한 시도일수록 반드시 남긴다.

```
2026-08-14  보상에 러더 사용량 항을 넣지 않았더니 타각이 지속 진동. 가중치 추가 검토 필요
2026-08-16  학습 조건 밖 초기 침로(180도)에서 성능 붕괴. 초기조건 랜덤화 추가
```

**두 파일은 작성자로 나눈다.** journal.md는 작업자 본인이 쓰고, AI에 위임한 작업의
기록은 worklog.md에 남긴다. journal.md에 AI가 쓴 내용을 넣지 않는다.

이 구분은 형식상의 정리가 아니다. 11장의 질문 "이 프로젝트에서 AI를 어디까지
사용했는가?"에 두 파일의 차이가 그대로 답이 된다. 근거는 D-009 참조.

### 커밋 메시지

무엇을 왜 바꿨는지 쓴다. `fix`, `update` 같은 메시지는 사용하지 않는다.

```
좋음:  보상 함수에 러더 사용량 페널티 추가 (타각 진동 억제)
좋음:  Nomoto 파라미터를 문헌 A의 표준 선형 값으로 교체
나쁨:  update model
```

`.slx`는 바이너리라 diff를 볼 수 없다. 모델을 수정한 커밋은 변경 내용을 메시지에 상세히 남긴다.

### .gitignore

학습 체크포인트, 대용량 `.mat`, 임시 파일은 제외한다.
`results/`에는 그래프와 요약 지표만 커밋한다.

---

## 7. 평가 지표

침로 제어 성능은 다음 지표로 평가한다.

| 지표 | 설명 | 보상 가중치 |
|---|---|---|
| 침로 오차 | 목표 침로와의 정상상태 편차 (RMS) | `[TODO]` |
| 정정 시간 | 목표 침로의 ±5% 이내 진입까지 소요 시간 | `[TODO]` |
| 오버슈트 | 최대 초과 각도 | `[TODO]` |
| 러더 사용량 | 타각 변화율의 적분 (구동기 부하) | `[TODO]` |

> **가중치는 작업자가 직접 결정한다.**
> 이 가중치를 정하는 것이 곧 "무엇을 좋은 제어라고 정의하는가"에 대한 답이며, 이 프로젝트의 핵심 설계 결정이다.
> 결정 시 `decisions.md`에 근거를 남긴다.
>
> 참고: 침로 오차만 최소화하면 러더가 지속 진동하는 해가 나오기 쉽다. 구동기 부하 항을 반드시 포함할 것.

---

## 8. 검증 요구사항

"학습이 잘 됐다"는 것만으로는 결과를 채택하지 않는다. 아래를 모두 확인한다.

1. **베이스라인 대비 비교** — 작업자가 수동 튜닝한 PID와 동일 조건에서 비교. 비교표는 반드시 결과물에 포함
2. **미학습 조건 재현** — 학습에 사용하지 않은 초기 침로, 목표값, 외란 크기에서 성능이 유지되는지 확인
3. **구동기 거동 확인** — 성능 지표가 좋아도 타각이 진동하면 채택하지 않는다. 시간 응답 그래프를 항상 함께 확인
4. **재현성** — 랜덤 시드를 바꿔 학습을 반복했을 때 결과가 유사한지 확인. 시드 하나의 결과만으로 결론 내지 않는다

---

## 9. 금지 사항

- ❌ 베이스라인 PID를 자동 튜닝 스크립트로 대체하는 것
- ❌ 보상 함수 가중치를 임의로 설정하고 진행하는 것
- ❌ 문헌 계수를 출처 없이 사용하는 것
- ❌ 검증 없이 "학습 수렴" 만으로 결과를 채택하는 것
- ❌ 단계를 건너뛰고 3자유도부터 시작하는 것
- ❌ 잠수정(UUV) 구현으로 범위를 넓히는 것 — 문서상 확장 과제로만 언급

---

## 10. 첫 세션 착수 지시

아래 순서로 진행한다.

1. 저장소 초기화 및 위 폴더 구조 생성, `.gitignore` 작성
2. `docs/01_requirements.md` 초안 작성 — 이 문서 1~4장 내용을 기반으로 하되, 판단이 필요한 항목은 빈칸
3. MATLAB 환경 확인 (버전, Simulink, RL Toolbox 라이선스) → 3장의 `[TODO]` 채우기
4. Nomoto 1차 모델 구현 및 계단 응답 확인
   - 문헌에서 K, T 표준 값을 가져오고 출처를 `docs/02_plant_model.md`에 기록
   - 타각 계단 입력에 대한 선수각속도 응답이 물리적으로 타당한지 확인
5. **여기서 멈춘다.** 베이스라인 PID 튜닝은 작업자가 직접 수행한다.
   - 튜닝용 스크립트와 응답 플롯 도구는 준비해두되, 게인 값은 작업자가 입력
   - 작업자가 튜닝을 마치고 `docs/03_baseline_pid.md`에 결과를 기록한 뒤 다음 단계로 진행

---

## 11. 참고 — 면접에서 나올 질문

이 프로젝트는 면접 소재로 쓰인다. 아래 질문에 답할 수 있는 형태로 결과물을 남긴다.

- 왜 잠수정이 아니라 수상정인가?
- 파랑 때문에 수상정이 더 복잡하지 않은가?
- 보상 함수 가중치는 왜 그렇게 정했는가?
- RL이 낸 게인을 실제 해상에서 신뢰할 수 있는가?
- 이 프로젝트에서 AI를 어디까지 사용했는가?
- 가장 고생한 부분은 무엇인가?

마지막 두 질문의 답은 `decisions.md`와 `journal.md`에서 나온다. 그래서 이 두 파일이 코드만큼 중요하다.

- **"가장 고생한 부분은 무엇인가?"** → `journal.md`
- **"AI를 어디까지 사용했는가?"** → `decisions.md`의 `AI 활용` 항목, 그리고 `journal.md`와 `worklog.md`의 경계
